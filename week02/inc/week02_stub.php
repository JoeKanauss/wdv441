<?php
/*
	user_id
	first_name
	last_name
	email
	phone
	password
*/	

function load(user_id)
{
	// use the user_id to search for and load any records that it matches
}

function save(user_id, first_name, last_name, email, phone, password)
{
	// using the record that matches the user_id, update the long-term storage with the first_name, last_name, email, phone, and password values
}

function validate(first_name, last_name, email, phone, password)
{
	// using validation functions, validate the first_name, last_name, email, phone, and password values
}
?>